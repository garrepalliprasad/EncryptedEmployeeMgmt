@model IEnumerable<Employee>
@{
    ViewData["Title"] = "Employee List Page";
}
<table class="table table-striped">
    <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>DOJ</th>
            <th style="width:25%">CTC</th>
            <th style="width:25%">Salary</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var employee in Model)
        {
            <tr>
                <td>@employee.Id</td>
                <td>@employee.Name</td>
                <td>@employee.DOJ</td>
                <td style="width:25%">@employee.CTC</td>
                <td style="width:25%">@employee.Salary</td>
                <td>
                    <a asp-action="SalaryHistory" asp-controller="Employee" asp-route-Eid=@employee.Id>Show Salary History</a>
                </td>
            </tr>
        }
    </tbody>
</table>